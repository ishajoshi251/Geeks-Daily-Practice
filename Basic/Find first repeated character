class Solution:

    def firstRepChar(self, s):
        # code here
        a=[]
        for i in s:
            if i in a:
                return i
            else:
                a.append(i)
        return -1
