class Solution:
    def areElementsContiguous (self, arr, n): 
    #Complete the function
        s = list(set(arr))
        for i in range(len(s)-1):
            if s[i] + 1 != s[i+1]:
                return False
        return True
