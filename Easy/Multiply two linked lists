def multiplyTwoList(head1, head2):
    # Code here
    a=b=0
    itr = head1
    ptr = head2
    while itr:
        a = a*10 + itr.data
        itr = itr.next
    while  ptr:
        b = b*10 + ptr.data
        ptr = ptr.next
    return (a*b)%MOD
