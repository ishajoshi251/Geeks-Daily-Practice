class Solution:
    def indexes(self, v, x):
        # Your code goes here
        li = []
        if x not in v:
            return [-1,-1]
        else:
            for i in range(len(v)):
                if v[i] == x:
                    li.append(i)
        return min(li) , max(li)
