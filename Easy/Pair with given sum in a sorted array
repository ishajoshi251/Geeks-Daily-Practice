class Solution:
    def Countpair (self, arr, n, sum) : 
        #Complete the function
        count =0
        for i in range(n):
            for j in range(i+1,n):
                if arr[i] + arr[j] == sum:
                    count += 1
        if count == 0:
            return count-1
        return count
